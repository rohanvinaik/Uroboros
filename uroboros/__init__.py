"""Uroboros — the serpent that eats convoluted code until only pure behaviour remains.

A set-and-forget crawler that drives Detective (over Wesker) function by function: break
tangled code at proven seams, pin behaviour with a minimal test suite, and ruthlessly execute
unworthy mutants — cycling until each function is as clean as mutation-pinning of its semantic
degrees of freedom can make it. No intelligence of its own; a small local model is a pure
function at exactly one step. Driven by Wesker et al, the mindless cycle refines toward purity.
"""

__version__ = "0.1.0"
